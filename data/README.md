The master dataset has been compiled and saved as the dataframe named "df" within the code. To replicate the research, simply import the data as "df", and the remaining code will execute accordingly.
